APUNTES PARA COMANDOS
git add . //es para agregar los archivos
git pull origin main //es para traer de la nuve al local
git push origin main //es para mandar del local a la nuve
git init // para agregar una nueva carpeta
git remote add origin (link del repositorio) //nos sirve para poder usar nuestro repositorio.
gitk ayuda a abir un visual de las ramas de git
nota: para poder volver a una carpeta anterior usamos "cd .."
nota: para poder  ir a una carpeta  usamos "cd (nombre de la carpeta o archivo)"
nota: para poder crear a una carpeta  usamos "mkdir" y añadimos el nombre de nuestro archivo
para cambiar de master a main :     git branch -m main

